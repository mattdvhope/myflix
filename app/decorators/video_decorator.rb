class VideoDecorator < Draper::Decorator

  # def rating_average
  #         - if @video.rating_average > 0
  #           = "#{@video.rating_average} / 5"
  #         - else
  #           "No rating yet"
  # end

end